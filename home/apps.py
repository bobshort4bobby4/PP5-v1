"""
    settings for app configuration for home app
"""


from django.apps import AppConfig


class HomeConfig(AppConfig):
    """
    settings for app configuration for home app
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'home'
